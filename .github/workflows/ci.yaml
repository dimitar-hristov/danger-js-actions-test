name: CI Pull Requests
on:
  pull_request:
    branches: [main]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@master
      - name: Danger
        uses: docker://ghcr.io/danger/danger:master
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
